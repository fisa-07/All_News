<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AllNews" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/my_primary</item>
    </style>

    <style name="Theme.AllNews" parent="Base.Theme.AllNews" />

    <style name="Base.Theme.splashAllNews" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:windowBackground">@color/my_primary</item>
        <item name="colorPrimary">@color/my_primary</item>
    </style>

    <style name="Theme.splashAllNews" parent="Base.Theme.AllNews" />
</resources>